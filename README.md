A fast, offline-first to‑do app built with HTML, CSS, and vanilla JavaScript. Tasks persist in the browser via localStorage, can be reordered with drag‑and‑drop, viewed as list or cards, and support due date/time, filters, sort, bulk deletes, and one‑click undo.
